---
phase: 01-foundation
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/index.html
  - frontend/src/index.css
  - frontend/src/main.jsx
  - frontend/src/App.jsx
  - frontend/src/components/Sidebar.jsx
  - frontend/src/components/Navbar.jsx
  - frontend/src/components/Layout.jsx
autonomous: true
requirements: [PROJ-02]

must_haves:
  truths:
    - "Vite frontend runs with responsive desktop/mobile navbar & sidebar"
    - "Aesthetic design system loaded via CSS variables matches color guide (Orange #FF6B00, Graphite #1E1E1E)"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "Global styles and custom properties (CSS variables)"
    - path: "frontend/src/components/Layout.jsx"
      provides: "Main dashboard framework grid wrapping content with Sidebar/Navbar"
---

<objective>
Setup React + Vite frontend, configure standard Vanilla CSS custom variables system for rich premium construction branding aesthetics, and build the persistent layout (Sidebar + Navbar).
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Setup Frontend configuration and dependencies</name>
  <files>frontend/package.json, frontend/index.html</files>
  <action>Create frontend/package.json with dependencies: react, react-dom, react-router-dom, lucide-react, chart.js, react-chartjs-2. Update index.html to include Poppins and Inter Google fonts.</action>
  <verify>frontend/package.json contains necessary libraries</verify>
  <done>Frontend package.json and fonts initialized</done>
</task>

<task type="auto">
  <name>Task 2: Build Design System in CSS</name>
  <files>frontend/src/index.css</files>
  <action>Define CSS variables on :root: --primary-orange (#FF6B00), --dark-graphite (#1E1E1E), --bg-light (#F5F5F5), --steel-blue (#2F80ED), --success-green (#27AE60), --alert-red (#EB5757), --white (#FFFFFF), and border-radius / spacing scales. Add basic body styles using Inter font, and headings using Poppins.</action>
  <verify>index.css contains color variables and global body setup</verify>
  <done>Premium CSS theme tokens configured</done>
</task>

<task type="auto">
  <name>Task 3: Build Navigation Layout components</name>
  <files>frontend/src/components/Sidebar.jsx, frontend/src/components/Navbar.jsx, frontend/src/components/Layout.jsx, frontend/src/App.jsx, frontend/src/main.jsx</files>
  <action>Create Sidebar (logo, navigation links: Dashboard, Projects, Workers, Materials, Expenses, Reports, Settings). Create Navbar (showing active page title, current user information, logout button). Create Layout wrapping Sidebar, Navbar, and content outlet. Configure in App.jsx and mount in main.jsx.</action>
  <verify>Layout builds and displays navigation headers correctly</verify>
  <done>Navigation framework and core dashboard shell created</done>
</task>
</tasks>

<verification>
- [ ] React project builds without lint/typescript errors
- [ ] CSS colors match style requirements
</verification>

<success_criteria>
- App layout matches branding guidelines
- Renders responsive panels
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
